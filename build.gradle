plugins {
    id "net.ltgt.apt" version "0.9"
}

group 'net.kleinhaneveld.cli'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'maven'

sourceCompatibility = 1.7

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile group: 'net.kleinhaneveld.fn', name: 'fn', version: '1.0-SNAPSHOT'
    compile group: 'org.immutables', name: 'value', version: '2.3.2'
    testCompile group: 'org.hamcrest', name: 'hamcrest-all', version: '1.3'
    testCompile group: 'junit', name: 'junit', version: '4.12'
    testCompile group: 'org.mockito', name: 'mockito-all', version: '1.9.5'

    compileOnly "org.immutables:value:2.3.2:annotations"
    apt         "org.immutables:value:2.3.2"
}

uploadArchives {
    repositories {
        mavenLocal()
    }
}